apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "privoxy-vpn.fullname" . }}-test-connection"
  labels:
    {{- include "privoxy-vpn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "privoxy-vpn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
